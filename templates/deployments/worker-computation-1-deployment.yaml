apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-computation-1
  namespace: {{ .Values.namespace }}
  labels:
    app: worker-computation-1

spec:
  replicas: {{ .Values.workerComputation1.replicaCount }}

  selector:
    matchLabels:
      app: worker-computation-1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%

  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

  template:
    metadata:
      labels:
        app: worker-computation-1

    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecrets.name }}

      securityContext: {}

      containers:
        - name: worker-computation-1
          image: "{{ .Values.workerComputation1.image }}"
          imagePullPolicy: Always

          args:
            - /rqworker/start-computation-1

          resources:
            requests:
              cpu: {{ .Values.workerComputation1.resources.requests.cpu }}
              memory: {{ .Values.workerComputation1.resources.requests.memory }}
            limits:
              cpu: {{ .Values.workerComputation1.resources.limits.cpu }}
              memory: {{ .Values.workerComputation1.resources.limits.memory }}

          env:
            - name: DATABASE_URL
              value: {{ .Values.workerComputation1.databaseUrl | quote }}
            - name: REDIS_URL
              value: {{ .Values.workerComputation1.redisUrl | quote }}

          envFrom:
            - configMapRef:
                name: {{ .Values.workerComputation1.configMaps.web }}
            - configMapRef:
                name: {{ .Values.workerComputation1.configMaps.postgres }}
            - secretRef:
                name: {{ .Values.workerComputation1.secrets.web }}
            - secretRef:
                name: {{ .Values.workerComputation1.secrets.postgres }}

          volumeMounts:
            - name: static-storage
              mountPath: /opt/revolutio/static

            - name: templates-storage
              mountPath: /opt/revolutio/Core/templates/user_defined_template

            - name: platformconfig-storage
              mountPath: /opt/revolutio/Platform_Configs

            - name: computation-storage
              mountPath: /opt/revolutio/Computation_Temp_Storage

            - name: media-storage
              mountPath: /opt/revolutio/Core/media

          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL

      volumes:
        - name: static-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation1.pvc.static }}

        - name: templates-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation1.pvc.templates }}

        - name: platformconfig-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation1.pvc.platformConfigs }}

        - name: computation-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation1.pvc.computation }}

        - name: media-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation1.pvc.media }}

      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst