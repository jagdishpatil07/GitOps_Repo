apiVersion: apps/v1
kind: Deployment
metadata:
  name: saml
  namespace: {{ .Values.namespace }}
  labels:
    service: saml

spec:
  replicas: 1

  selector:
    matchLabels:
      service: saml

  template:
    metadata:
      labels:
        service: saml

    spec:
      securityContext:
        runAsUser: 70070
        runAsNonRoot: true
        fsGroup: 70070

      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecrets.name }}

      ########################################################
      # ✅ VOLUMES
      ########################################################
      volumes:
        - name: platformconfig-storage
          persistentVolumeClaim:
            claimName: {{ .Values.saml.pvc.platformConfigs }}

        - name: static-storage
          persistentVolumeClaim:
            claimName: {{ .Values.saml.pvc.static }}

        - name: media-storage
          persistentVolumeClaim:
            claimName: {{ .Values.saml.pvc.media }}

        - name: assets-storage
          persistentVolumeClaim:
            claimName: {{ .Values.saml.pvc.assets }}

      ########################################################
      # ✅ CONTAINER
      ########################################################
      containers:
        - name: saml
          image: "{{ .Values.saml.image }}"
          imagePullPolicy: Always

          securityContext:
            runAsUser: 70070

          resources:
            requests:
              cpu: {{ .Values.saml.resources.requests.cpu }}
              memory: {{ .Values.saml.resources.requests.memory }}
            limits:
              cpu: {{ .Values.saml.resources.limits.cpu }}
              memory: {{ .Values.saml.resources.limits.memory }}

          ports:
            - name: uvicorn
              containerPort: 9000

          ########################################################
          # ✅ REQUIRED ENV (ONLY THIS ONE IS MANDATORY)
          ########################################################
          env:
            - name: DATABASE_URL
              value: {{ .Values.web.databaseUrl | quote }}
            - name: DJANGO_SETTINGS_MODULE
              value: "saml.settings"

          ########################################################
          # ✅ CONFIGMAP + SECRETS (SAME AS WEB)
          ########################################################
          envFrom:
            - configMapRef:
                name: {{ .Values.web.configMaps.web }}
            - configMapRef:
                name: {{ .Values.web.configMaps.postgres }}
            - secretRef:
                name: {{ .Values.web.secrets.web }}
            - secretRef:
                name: {{ .Values.web.secrets.postgres }}

          ########################################################
          # ✅ VOLUME MOUNTS
          ########################################################
          volumeMounts:
            - name: platformconfig-storage
              mountPath: /opt/revolutio/Platform_Configs

            - name: media-storage
              mountPath: /opt/revolutio/Core/media

            - name: static-storage
              mountPath: /opt/revolutio/static

            - name: assets-storage
              mountPath: /opt/revolutio/assets

          ########################################################
          # ✅ FINAL START (EXACT SAME AS WEB + SINGLE WORKER)
          ########################################################
          command:
            - /bin/bash
          args:
            - -c
            - |
              echo "====== BOOTSTRAP START ======"

              # Start bootstrap
              /Installation_ref/scripts/start &

              sleep 20

              echo "====== SWITCHING TO SINGLE WORKER ======"

              # kill multiple workers
              pkill -f uvicorn || true

              # start single worker with correct settings
              exec uvicorn saml.asgi:application \
                --host 0.0.0.0 \
                --port 9000 \
                --workers 1 \
                --lifespan off \
                --timeout-keep-alive 60 \
                --log-level info

          ########################################################
          # ✅ PROBES (SAME AS WEB)
          ########################################################
          readinessProbe:
            httpGet:
              path: /
              port: uvicorn
            initialDelaySeconds: 120
            periodSeconds: 20

          livenessProbe:
            httpGet:
              path: /
              port: uvicorn
            initialDelaySeconds: 120
            periodSeconds: 30

      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst

  strategy:
    type: RollingUpdate
