apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-computation-0
  namespace: {{ .Values.namespace }}
  labels:
    app: worker-computation-0

spec:
  replicas: {{ .Values.workerComputation0.replicaCount }}

  selector:
    matchLabels:
      app: worker-computation-0

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%

  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

  template:
    metadata:
      labels:
        app: worker-computation-0

    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecrets.name }}

      # OpenShift safe
      securityContext: {}

      containers:
        - name: worker-computation-0
          image: "{{ .Values.workerComputation0.image }}"
          imagePullPolicy: Always

          args:
            - /rqworker/start-computation-0

          resources:
            requests:
              cpu: {{ .Values.workerComputation0.resources.requests.cpu | quote }}
              memory: {{ .Values.workerComputation0.resources.requests.memory }}
            limits:
              cpu: {{ .Values.workerComputation0.resources.limits.cpu | quote }}
              memory: {{ .Values.workerComputation0.resources.limits.memory }}

          env:
            - name: DATABASE_URL
              value: {{ .Values.workerComputation0.databaseUrl | quote }}
            - name: REDIS_URL
              value: {{ .Values.workerComputation0.redisUrl | quote }}

          envFrom:
            - configMapRef:
                name: {{ .Values.workerComputation0.configMaps.web }}
            - configMapRef:
                name: {{ .Values.workerComputation0.configMaps.postgres }}
            - secretRef:
                name: {{ .Values.workerComputation0.secrets.web }}
            - secretRef:
                name: {{ .Values.workerComputation0.secrets.postgres }}

          volumeMounts:
            - name: static-storage
              mountPath: /opt/revolutio/static

            - name: templates-storage
              mountPath: /opt/revolutio/Core/templates/user_defined_template

            - name: platformconfig-storage
              mountPath: /opt/revolutio/Platform_Configs

            - name: computation-storage
              mountPath: /opt/revolutio/Computation_Temp_Storage

            - name: media-storage
              mountPath: /opt/revolutio/Core/media

          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL

      volumes:
        - name: static-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation0.pvc.static }}

        - name: templates-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation0.pvc.templates }}

        - name: platformconfig-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation0.pvc.platformConfigs }}

        - name: computation-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation0.pvc.computation }}

        - name: media-storage
          persistentVolumeClaim:
            claimName: {{ .Values.workerComputation0.pvc.media }}

      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst