apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbitmq-pvc-1
  namespace: myapp
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: rabbitmq-pv-1
  resources:
    requests:
      storage: 10Gi
