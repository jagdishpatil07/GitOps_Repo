apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-conf-pvc-1
  namespace: myapp

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: ""

  volumeName: nginx-conf-pv-1   # ✅ STRICT BINDING

  resources:
    requests:
      storage: 5Gi

  volumeMode: Filesystem