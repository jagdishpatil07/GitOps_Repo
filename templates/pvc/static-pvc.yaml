apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-pvc-1
  namespace: myapp

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: ""

  volumeName: static-pv-1   # ✅ STRICT BINDING

  resources:
    requests:
      storage: 10Gi

  volumeMode: Filesystem