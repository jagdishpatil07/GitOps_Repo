apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc-1
  namespace: myapp

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: ""

  volumeName: media-pv-1   # ✅ STRICT BINDING

  resources:
    requests:
      storage: 5Gi

  volumeMode: Filesystem