apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-defined-template-pvc-1
  namespace: myapp

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: ""

  volumeName: user-defined-template-pv-1   # ✅ STRICT BINDING

  resources:
    requests:
      storage: 5Gi
