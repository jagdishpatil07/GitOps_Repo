apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-data-new-pvc-11
  namespace: myapp

spec:
  accessModes:
    - ReadWriteMany   # ✅ FIXED

  resources:
    requests:
      storage: 5Gi

  volumeName: postgresql-nfs-pv
  storageClassName: ""
