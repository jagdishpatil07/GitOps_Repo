apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-data-new-pvc-11
  namespace: myapp

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi

  volumeName: postgresql-nfs-pv
  storageClassName: ""
