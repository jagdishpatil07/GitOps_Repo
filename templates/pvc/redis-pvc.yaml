apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis-1
  namespace: myapp

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: ""

  volumeName: redis-pv-1   # ✅ FIX

  resources:
    requests:
      storage: 5Gi

  volumeMode: Filesystem