apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couchdb-pvc-1
  namespace: myapp
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: couchdb-pv-1
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
