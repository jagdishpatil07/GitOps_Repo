apiVersion: v1
kind: Service
metadata:
  name: keydb
  namespace: {{ .Release.Namespace }}
  labels:
    service: keydb
spec:
  selector:
    service: keydb
  ports:
    - name: keydb
      port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP