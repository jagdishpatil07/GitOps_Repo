apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ .Release.Namespace }}
  labels:
    service: redis

spec:
  selector:
    service: redis

  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP

  type: ClusterIP