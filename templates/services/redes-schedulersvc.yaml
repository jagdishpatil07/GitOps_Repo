apiVersion: v1
kind: Service
metadata:
  name: redis-scheduler
  namespace: {{ .Release.Namespace }}
  labels:
    service: redis-scheduler

spec:
  selector:
    service: redis-scheduler

  ports:
    - name: redis-scheduler
      port: 6379
      targetPort: 6379
      protocol: TCP

  type: ClusterIP