apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql-nfs-pv

spec:
  capacity:
    storage: 5Gi

  accessModes:
    - ReadWriteOnce

  nfs:
    path: /data/nfs/postgresql-data-new
    server: 127.0.0.1   # ✅ your node IP

  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
